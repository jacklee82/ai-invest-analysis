# 단계별 세부 계획: Better‑T‑Stack 스캐폴딩 + UI‑First 구현

본 계획은 Better‑T‑Stack Variant(Next.js, tRPC, Drizzle ORM, Tailwind CSS, Turborepo, Bun) 기반으로 프런트 UI부터 설계/구현하는 단계적 실행안을 제시한다. DB는 MVP 단계에서 SQLite를 사용하고, 필요 시 PostgreSQL로 전환한다. FastAPI(예측) 연동은 후속 단계에서 점진적으로 추가한다. 파이썬 환경은 uv(Astral)를 사용한다.

---

## 1. 원칙(Principles)

- **UI‑First, Data‑Backed**: 먼저 화면/흐름을 확정, 이후 API/DB를 채운다.
- **모듈 경계 유지**: Web(tRPC) ↔ AI(FastAPI)는 REST 계약으로 분리.
- **관측가능성**: 성능/오류 지표를 초기부터 수집.
- **A2A 검증 내재화**: 업로드/ETL 시 스키마/도메인 검증 포함.
- **문서 동기화**: 변경사항은 `summary.md`와 동기화, ADR 로그 유지.
- **PDF 취득/열람**: OCR/문서 열람은 Edge가 아닌 Adobe Acrobat 사용.

---

## 2. 사전 준비(Prereqs)

- Node.js 20.x, `Bun` 최신
- Git, Turborepo CLI
- Python 3.11+, `uv`(패키지/가상환경)
- Git 리포지토리 초기화, CI(예: GitHub Actions) 권한
- DB: SQLite(MVP), `.env` 템플릿 준비

---

## 3. 스캐폴딩(모노레포 골격)

### 3.1 모노레포 생성(Turborepo)

```bash
# Turborepo + Bun
mkdir ai-invest && cd ai-invest
bunx create-turbo@latest

# app 생성 (apps/web)
cd apps
bunx create-next-app@latest web --ts --eslint --tailwind --src-dir --app --import-alias "@/*"
cd ../

# 공통 패키지
mkdir -p packages/config packages/tsconfig

# 의존성(루트)
bun add -D turbo typescript @types/node prettier eslint

# 웹 앱 의존성
cd apps/web
bun add @trpc/server @trpc/client @trpc/react-query @tanstack/react-query zod superjson
bun add class-variance-authority tailwind-merge lucide-react date-fns
bun add echarts echarts-for-react

# Drizzle + SQLite
bun add drizzle-orm drizzle-kit better-sqlite3 dotenv

# Git 초기화
cd ../../
git init
```

### 3.2 모노레포 구조

- `apps/web`: Next.js + tRPC
- `packages/config`: eslint/prettier/tailwind 공유 설정
- `packages/tsconfig`: `base.json`, `next.json`
- `turbo.json`: 파이프라인 정의(build, lint, dev)

### 3.3 Tailwind 설정 확인

- `tailwind.config.ts`, `postcss.config.js`, `globals.css` 정상 생성 확인
- 프레임워크 스타일 토큰(색상/타이포/간격) 선언

### 3.4 tRPC 기본 배선

- `src/server/trpc.ts`: tRPC `initTRPC` + `superjson` + 에러 포맷터
- `src/server/routers/_app.ts`: 루트 라우터
- `src/pages/api/trpc/[trpc].ts`: Next API 핸들러
- `src/utils/trpc.ts`: 클라이언트 생성기 + React Query Provider

### 3.5 Drizzle + SQLite 초기화

`.env` 예시

```env
DATABASE_URL="file:./ai_invest.db"
```

`drizzle.config.ts` 예시

```ts
import type { Config } from "drizzle-kit";

export default {
  schema: "./src/server/db/schema.ts",
  out: "./drizzle",
  driver: "better-sqlite",
  dbCredentials: { url: process.env.DATABASE_URL! },
} satisfies Config;
```

`src/server/db/schema.ts` 예시(발췌)

```ts
import { sqliteTable, text, integer } from "drizzle-orm/sqlite-core";

export const project = sqliteTable("project", {
  projectId: text("project_id").primaryKey(),
  projectName: text("project_name").notNull().unique(),
  companyName: text("company_name").notNull(),
  businessType: text("business_type").notNull(),
  contractStart: text("contract_start_date").notNull(),
  baseMonths: integer("base_contract_months").notNull(),
  extendedMonths: integer("extended_months").notNull(),
  initialInvestment: integer("initial_investment").notNull(),
  additionalInvestment: integer("additional_investment").notNull(),
  totalRecouped: integer("total_recouped").notNull(),
  mdPurchaseCost: integer("md_purchase_cost"),
  ostInvestment: integer("ost_investment"),
});

export const cashflowMonthly = sqliteTable("cashflow_monthly", {
  id: text("id").primaryKey(),
  projectId: text("project_id").notNull(),
  yyyymm: text("yyyymm").notNull(),
  revenue: integer("revenue_amount").notNull(),
  cost: integer("cost_amount").notNull(),
  recoup: integer("recoup_amount").notNull(),
});
```

마이그레이션

```bash
# 스키마 → SQL 생성
bunx drizzle-kit generate
# DB 반영
bunx drizzle-kit push
```

---

## 4. 정보구조(IA) & 라우팅 맵

- `/dashboard`  경영 KPI 홈(총계, YoY, 사업 비교, 워터폴, 리스크 현황, Top 기획사)
- `/risk`       리스크 목록/필터/정렬 + 상세 팝업(최근 3개월 미니 차트)
- `/simulation` 구보 시뮬레이션 입력 → Best/Mod/Worst DCF/BEP 결과
- `/analysis`   차트인 가치/기획사 ROI/코호트/파레토/분포 리포트
- `/upload`     A/B 파일 업로드(A2A 검증, 이력/해시, 진행상태)

글로벌: 상단 네비, 기간/사업타입/기획사 필터 바, 사용자 힌트/툴팁

---

## 5. 디자인 시스템(초안)

- 토큰: 색상 팔레트, 간격(scale), 타이포(크기/가중치), 그림자
- 컴포넌트: `AppShell`, `Card`, `Kpi`, `Table`, `Badge`, `Tooltip`, `Modal`, `Tabs`, `Dropdown`, `EmptyState`, `ErrorState`, `Skeleton`
- 차트 컴포넌트: `Sparkline`, `Donut`, `AreaLine`, `GroupedBar`, `Waterfall`, `Scatter`, `Heatmap`, `BoxPlot`
- 접근성: 대비/키보드 포커스/ARIA, 축 라벨 단위 자동 스케일(억/만)

---

## 6. 페이지별 UI 설계(와이어 디테일)

### 6.1 `/dashboard`
- 1행: KPI 3 + 리스크 도넛(전년 동월 대비)
- 2행: 사업별 성과 그룹 막대(매출/이익) + 월별 추세(면적+이익률 선)
- 3행: 투자→회수 워터폴(좌) + Top 기획사 스캐터(우)
- 상호작용: 범례 토글, 막대/점 클릭 드릴다운, 툴팁 상세

### 6.2 `/risk`
- 테이블: 프로젝트/기획사/상태/회수율/경과/만기/최근 추이 스파크라인
- 필터: 사업타입/회수율 범위/만기 임박/기간
- 상세: 팝업(총 투자액, 현재 회수율, 계약 기간, 최근 3개월 추이)

### 6.3 `/simulation`
- 입력: 최근 12개월 월 수익 배열, 계약 개월 수, 모델(auto/ARIMA/Prophet)
- 결과: Best/Mod/Worst CF/DCF/BEP 카드 + 곡선 비교 차트
- 내보내기: 결재 양식과 호환되는 표/이미지

### 6.4 `/analysis`
- 탭: 차트인 가치, 기획사 ROI, 코호트, 분포/상자그림, 파레토
- 각 탭: 상단 필터 + 주요 차트 1~2 + 표(내려받기)

### 6.5 `/upload`
- Dropzone → 미리보기 → A2A 검증 결과(필수 컬럼/타입/도메인) → 적재 진행률
- 업로드 이력: 파일명/해시/행수/소요/상태/에러 메시지
- 문서 열람/증빙: Adobe Acrobat으로 PDF 열람 가이드 링크

---

## 7. 프런트 데이터 전략

- tRPC + React Query: 쿼리키 규칙, 스켈레톤/에러/리트라이 정책
- 캐시 TTL: 대시보드 핵심 쿼리 5분, 상세/테이블 60초, 즉시성 필요 시 무효화
- 차트 데이터: 사전 집계 뷰 우선 조회, TopN 제한 및 lazy-load

---

## 8. tRPC 라우터 스텁(프런트 계약)

```ts
// src/server/routers/_app.ts
export const appRouter = createTRPCRouter({
  dashboard: dashboardRouter,
  risk: riskRouter,
  analysis: analysisRouter,
  simulation: simulationRouter,
  upload: uploadRouter,
});
export type AppRouter = typeof appRouter;
```

```ts
// src/server/routers/dashboard.ts
export const dashboardRouter = router({
  getSummary: publicProcedure.query(() => {/* ... */}),
  getBusinessComparison: publicProcedure.query(() => {/* ... */}),
  getTrends: publicProcedure.query(() => {/* ... */}),
});
```

```ts
// src/server/routers/risk.ts
export const riskRouter = router({
  listWarnings: publicProcedure.input(z.object({ page: z.number().min(1) })).query(() => {/* ... */}),
});
```

```ts
// src/server/routers/simulation.ts
export const simulationRouter = router({
  forecast: publicProcedure
    .input(z.object({ series: z.array(z.number()).length(12), months: z.number().min(1).max(36), model: z.enum(["auto","arima","prophet"]) }))
    .mutation(() => {/* FastAPI 연동 */}),
});
```

```ts
// src/server/routers/upload.ts
export const uploadRouter = router({
  startJob: publicProcedure.input(z.object({ fileMeta: z.string() })).mutation(() => {/* ... */}),
  finishJob: publicProcedure.input(z.object({ jobId: z.string() })).mutation(() => {/* ... */}),
});
```

---

## 9. Drizzle 스키마(초안)

- 위치: `apps/web/src/server/db/schema.ts` (위 예시 참조)
- 마이그레이션: `bunx drizzle-kit generate && bunx drizzle-kit push`

---

## 10. 시드 & 샘플 데이터팩

- `scripts/seed.ts`에서 소형 A/B 예제 적재(3~5개 프로젝트, 12개월 집계)
- `bun run db:seed` 스크립트 등록
- 업로드 플로우와 별개로 즉시 대시보드 시연 가능

---

## 11. 업로드 UI & A2A 검증 통합

- 스텝 UI: 파일 선택 → 미리보기 → 검증 결과 → 적재 실행 → 결과 요약
- 검증: 필수 컬럼/타입/도메인, `프로젝트명` 매핑, 금액 ≥ 0, 날짜 유효성
- 파일 해시/이력 표기, 실패 시 전체 롤백 원칙 명시

---

## 12. 차트 구현 순서(우선순위)

1) `/dashboard` KPI 카드/스파크라인 → 사업별 막대 → 월별 추세 → 워터폴 → 스캐터
2) `/risk` 테이블+스파크라인 → 만기 히트맵 → 경고 분해 막대
3) `/analysis` 차트인 회귀 → 파레토 → 상자그림 → 코호트
4) `/simulation` 결과 곡선/카드 → 입력 UX → 내보내기

권장 라이브러리: ECharts(대용량/상호작용/커스터마이즈 유연)

---

## 13. FastAPI(예측) 서비스 스켈레톤(후속)

```bash
# Python(FastAPI) – uv 사용
uv venv
uv pip install fastapi uvicorn[standard] pydantic numpy pandas statsmodels prophet
```

엔드포인트(초안): `POST /forecast { series[12], months<=36, model }`
응답: `{ moderate: number[], meta: { model, rmse, mape } }`

---

## 14. 수용 기준(샘플)

- 대시보드 첫 화면 p95 < 500ms(캐시)
- 업로드 10만 행 처리 ≤ 합리적 시간, 검증 실패 시 롤백
- 예측 API p95 < 2s, 실패율 < 0.5%
- BEP 오차 중앙값 ≤ 2개월(샘플셋 기준)
- 접근성: 키보드 내비, 색 대비 준수

---

## 15. 마일스톤(주차 기준)

- **M1: 스캐폴딩/IA/디자인 시스템**
  - Turborepo+Bun 세팅, Next/tRPC/Tailwind 설치, Drizzle/SQLite 구성, 레이아웃/네비
- **M2: 대시보드 MVP**
  - KPI/막대/추세/워터폴/스캐터, 사전 집계 쿼리, 캐싱
- **M3: 리스크 MVP**
  - 테이블/스파크라인/필터, 경고 로직 계산, 만기 히트맵
- **M4: 업로드+A2A 검증**
  - 업로드 UI/이력, 파일 검증, 전체 덮어쓰기 방식 적재
- **M5: 시뮬레이터 연결**
  - FastAPI 스켈레톤 연동, DCF/BEP 계산, 내보내기
- **M6: 심층 분석**
  - 회귀/파레토/상자/코호트, 보고서 내보내기

---

## 16. 위험/완화

- 데이터 표준화 미흡 → 업로드 후 교차 매칭 리포트/경고
- 예측 정확도 저하 → 모델 폴백/하이브리드, 성능 메트릭 노출
- 성능 병목 → 머티리얼라이즈 뷰/인덱스/캐시/TopN 제한
- 조직 채택 저조 → 결재양식 일치/내보내기/교육/챔피언 사용자

---

## 17. CI/CD & 품질

- PR: 타입체크/ESLint/유닛테스트, 미리보기 빌드
- 린트/포맷터: TypeScript strict, ESLint, Prettier
- Python: ruff/black, API 계약 테스트
- 빌드/런너: `bun run build`, `bun run dev` 파이프라인
- 배포: Turborepo 파이프라인 → 온프렘/클라우드 릴리즈 스크립트

---

## 18. 체크리스트(실행용)

- [ ] Next/tRPC/Prisma/Tailwind 스캐폴딩 완료
- [ ] DB 마이그레이션/시드 완료
- [ ] `/dashboard` 위젯 5종 구현 및 p95 충족
- [ ] `/risk` 경고 목록/필터/만기 히트맵 구현
- [ ] `/upload` A2A 검증/이력/해시 구현
- [ ] `/simulation` FastAPI 연동, DCF/BEP 계산/내보내기
- [ ] 분석 차트 4종(회귀/파레토/상자/코호트) 구현
- [ ] 수용 기준/모니터링/KPI 대시보드 작성

---

본 계획은 `summary.md`와 연동되며, 변경 시 두 문서 동기화가 필요하다.


