# Project Scope: AI 기반 콘텐츠 투자 분석 시스템 (v0.2)

## 1. 프로젝트 개요 및 목적 (Overview & Purpose)

본 문서는 'AI 기반 콘텐츠 투자 분석 시스템'의 개발 범위를 정의합니다.

본 시스템의 목적은 분산된 '콘텐츠' BM(선급투자, 일반투자, OST, 음반)의 재무/정산 데이터를 통합하고, AI 및 통계 모델을 활용하여 **투자 의사결정(모듈 2)**을 지원하며, **투자 후 리스크(모듈 3)**를 체계적으로 관리하고, **경영진의 빠른 의사결정(모듈 1)**을 돕는 것입니다.

## 2. 사용자 및 우선순위 (Users & Priority)

본 시스템의 기능 개발 및 UI/UX 설계는 다음 사용자 우선순위를 따릅니다.

1.  **경영진:** (P1) 실시간 경영 현황 및 핵심 리스크 파악 (모듈 1)
2.  **사업관리/운영:** (P2) 투자 리스크 모니터링 및 성과 분석 (모듈 3, 4)
3.  **투자심사역:** (P3) 신규 투자를 위한 시뮬레이션 및 데이터 분석 (모듈 2)

* **권한:** 1단계(MVP)에서는 모든 사용자가 모든 모듈에 접근하는 것을 전제로 개발합니다. (권한 분리 Out of Scope)

## 3. 기술 스택 및 아키텍처 (Tech Stack & Architecture)

* **웹 애플리케이션:** Better‑T‑Stack Variant (Next.js, tRPC, Drizzle ORM, Tailwind CSS)
    * **패키지 매니저:** Bun
    * **모노레포:** Turborepo (apps/packages 구성)
    * 역할: UI/UX, 대시보드, 리스크 관리, 데이터 업로드
* **AI 백엔드:** Python (FastAPI), Pandas, Statsmodels/Prophet
    * 역할: 시계열 예측 모델 서빙 (모듈 2)
* **데이터베이스:** SQLite(MVP) → On‑prem/Cloud 시 PostgreSQL로 마이그레이션(Drizzle 지원)

## 4. 데이터 정의 및 관리 (Data Definition & Ops)

* **데이터 소스:**
    * `A파일 (요약)`: 프로젝트(투자) 단위의 마스터 데이터 (투자금, 회수금, 계약 기간, 연장 기간, 추가 투자금, 음반 매입원가 등)
    * `B파일 (원본)`: 일별/곡/앨범 단위의 정산 로우 데이터 (매출, 원가(지급금) 등)
* **데이터 키(Key):** `프로젝트명` (자유 텍스트). (데이터 정합성은 사전에 확보된 것으로 가정)
* **데이터 갱신 주기:** **월 1회** (Monthly)
* **갱신 방식:**
    1.  시스템 내 관리자용 **'엑셀 업로드' 페이지**를 통해 운영자가 직접 업로드 (`A` 방식)
    2.  업로드 파일은 특정 기간(예: 11월) 데이터가 아닌, **'누적 마스터 파일'** (`마스터` 방식)
    3.  업로드 시, 시스템은 기존 DB 데이터를 **모두 삭제(TRUNCATE)하고, 새 마스터 파일 데이터로 전체 덮어쓰기** (`A` 방식)

## 5. 핵심 기능 및 모듈 (Core Modules & Features)

### Module 1: 통합 대시보드 (P1 - 경영진)

경영진의 빠른 현황 파악을 위한 모듈.

* [ ] **1순위 (총계):** '콘텐츠' BM 전체의 핵심 지표 3가지
    * [ ] 누적 투자금
    * [ ] 누적 회수율 (%)
    * [ ] 현재 리스크 건수 (클릭 시 Module 3로 이동)
* [ ] **비교 기준:** 위 3가지 지표의 **작년 동기 대비(YoY)** 증감률 표시
* [ ] **2순위 (사업별 비교):** 4대 사업(선급투자, 일반투자, OST, 음반)별 성과 비교
    * [ ] 표시 지표: **매출액 / 이익액 / 이익률 (%)**
* [ ] **이익 계산 로직 (내부 구현):**
    * `선급투자`: 총회수금 - (최초 투자금 + 추가 투자금)
    * `일반투자`: 매출 - 원가 (B파일 내 컬럼 활용)
    * `음반(MD)`: 총매출 - 총 매입원가 (A파일 내 컬럼 활용)
    * `OST`: 매출 - 원가(투자금). (단, 투자금의 5년 감가상각 로직은 **낮은 우선순위(Low Priority)**로 개발)

### Module 2: 선급투자 시뮬레이터 (P3 - 투자심사)

AI 기반 투자 의사결정 지원.

* [ ] **범위 (MVP):** **'구보' 투자** 시뮬레이션 기능만 우선 개발. ('신보' 투자는 2단계 고도화)
* [ ] **[AI] 예측 모델 (FastAPI):**
    * Input: '구보'의 **최근 12개월** 월별 수익 데이터 (`C` 방식)
    * Model: **ARIMA, Prophet 등 통계/ML 시계열 모델** (`A` 방식)
    * Output: 사용자가 입력한 '미래 계약 기간(M개월)' 동안의 **월별 예상 현금 흐름(CF)** (예: `[1000, 950, 900, ...]`) (`A` 방식)
* [ ] **[WEB] 시뮬레이션 로직 (tRPC):**
    1.  AI가 예측한 `Moderate(100%) CF`를 수신
    2.  `Worst CF` (= Moderate CF * 0.9) 및 `Best CF` (= Moderate CF * 1.1) 자동 계산
    3.  `WACC`(고정값)를 적용하여, 3가지 CF(Worst, Moderate, Best)에 대한 **DCF를 각각 다시 계산**
* [ ] **[UI] 결과 화면:**
    * 투자심사역에게 **최종 요약 숫자**를 제시 (`A` 방식)
    * **예상 BEP** (개월)
    * **예상 DCF (Worst)**
    * **예상 DCF (Moderate)**
    * **예상 DCF (Best)**

### Module 3: 리스크 관리 시스템 (P2 - 사업관리)

선급투자의 사후 관리를 위한 모듈.

* [ ] **핵심 리스크 로직:**
    * `총 계약 기간` = `기본 계약 기간` + `연장 기간` (A파일)
    * `총 투자금` = `최초 투자금` + `추가 투자금` (A파일)
    * **'경고' 상태** 자동 분류: `(현재 - 계약 시작일) > (총 계약 기간 / 2)` 이면서 `(총회수금 / 총 투자금) < 0.5` 인 건
* [ ] **리스크 목록 UI:** '경고' 상태인 투자 건 목록을 테이블로 표시
* [ ] **상세 정보 (팝업):**
    * '경고' 건 클릭 시, 상세 정보 팝업 표시 (`A` 방식)
    * 팝업 내용: 총 투자액, 현재 회수율, 계약 기간(연장 포함), 기획사명 등
    * **핵심 기능:** 해당 건의 **'최근 3개월 월별 회수액' 미니 꺾은선 그래프(📈)**를 팝업에 포함 (`A` 방식)

### Module 4: 심층 분석 (P2 - 사업관리)

전략적 인사이트 도출.

* [ ] **'차트인' 성과 분석:**
    * `차트인 수익 정의`: 차트인(예: 100위권 내) 기간 동안 발생한 **모든 수익** (`A` 방식)
    * [ ] **기능 (B):** "차트 100위권 1주 = 평균 X천만원" 가치 추정 (단순 평균 및 AI 회귀 분석 모두 활용)
    * [ ] **기능 (C):** "차트인으로 가장 많은 수익을 낸 곡 Top 10" 리포트
* [ ] **'기획사' 성과 분석:**
    * [ ] **기능:** **'선급투자'를 받은** 기획사들만 필터링하여 **'투자 대비 회수율(ROI)'** 비교 (`A` 방식)
    * [ ] **정렬 기준:** 사용자가 선택 가능해야 함
        * `C`: **총 투자금액이 큰 순** (핵심 파트너)
        * `D`: **계약 만료일이 임박한 순** (락인(Lock-in) 대상)

## 6. 배포 및 발전 계획 (Deployment Plan)

단계별로 시스템을 발전시킴.

1.  **C (Local):** 로컬 PC에서 PoC(개념 증명) 및 핵심 기능(모듈 2 AI) 개발
2.  **B (On-premise):** 사내 서버에 배포하여 실 데이터 검증 및 내부 사용자(경영진/사업팀) 피드백 확보
3.  **A (Cloud):** (2단계 고도화 시) Vercel, AWS/GCP 등으로 이전하여 안정성 및 확장성 확보

## 7. 범위 외 (Out of Scope - MVP)

* '공연' BM 관련 모든 기능
* '신규' 아티스트 투자 시뮬레이션 (모듈 2 고도화)
* 사용자별 권한 분리
* 데이터 정합성 검증 및 클렌징 로직
* OST 감가상각 로직의 정교한 구현 (Low Priority)

## 8. 상세 요구사항 (Functional & Non-Functional)

* **기능 요구사항(요약):**
    * **대시보드:** 총계 3지표 + YoY + 사업별 비교, 리스크 건수 연동
    * **시뮬레이터:** 구보 12개월 데이터 입력 → 월별 예측 CF → WACC 적용 DCF 및 BEP 산출
    * **리스크:** 자동 규칙 기반 경고 분류, 테이블/팝업, 미니 꺾은선 그래프
    * **심층 분석:** 차트인 가치 추정, 기획사 ROI 비교 리포트
* **비기능 요구사항(NFR):**
    * **성능:**
        * **대시보드 응답시간:** p95 < 500ms (캐시 기준)
        * **리스트/리포트 조회:** p95 < 1.5s (페이지네이션/인덱스 활용)
        * **예측 API(모델 추론):** p95 < 2s (입력 12개월, 계약기간 6~36개월)
    * **가용성:** 사내 운영(B 단계) 근무시간 중 **99%**
    * **데이터 일관성:** 업로드 단위 트랜잭션(전부/전무), 업로드 실패 시 롤백
    * **감사/추적성:** 파일 업로드 이력, 누가/언제/무엇을(파일 해시) 업로드했는지 저장
    * **관측성:** 요청/오류 로그, 주요 KPI(예: 업로드 소요시간, 예측 실패율) 메트릭 수집
    * **보안:** 사내망 접근 제한(B 단계), 데이터베이스 최소권한 원칙, PII 최소화
    * **국제화:** 1단계 한국어 고정, 금액/날짜 포맷 KOR 표준
    * **브라우저 지원:** 최신 크롬/엣지(Chromium) 최신 2개 버전

## 9. 데이터 스키마 상세 (ERD/테이블)

* **핵심 엔티티:**
    * **`project` (프로젝트/투자 마스터)**
        * `project_id`(UUID), `project_name`(텍스트, 고유 가정), `company_name`, `contract_start_date`, `base_contract_months`, `extended_months`, `initial_investment`, `additional_investment`, `total_recouped`, `md_purchase_cost`, `ost_investment`, `business_type`(ENUM: 선급/일반/OST/음반), `updated_at`
    * **`cashflow_monthly` (월별 현금흐름/정산 집계)**
        * `project_id`, `yyyymm`, `revenue_amount`, `cost_amount`, `recoup_amount`, `created_at`
    * **`track` / `album` (선택, B파일 상세 연계 필요 시)**
        * `track_id`, `album_id`, `project_id`, `title`, `artist`, `is_charted`, `charted_weeks`, `created_at`
    * **`risk_flag` (리스크 평가 스냅샷)**
        * `project_id`, `evaluated_at`, `is_warning`, `reason`, `recoup_ratio`, `elapsed_ratio`
    * **`upload_job` (업로드/ETL 이력)**
        * `job_id`(UUID), `source`(A/B), `file_name`, `file_hash`, `rows_parsed`, `rows_loaded`, `status`(success/failed), `started_at`, `ended_at`, `error_message`

* **인덱스 권장:**
    * `cashflow_monthly(project_id, yyyymm)` 복합 인덱스 (SQLite에서는 가용한 범위로 적용, PostgreSQL 전환 시 강화)
    * `project(project_name)` 유니크 인덱스(데이터 가정 상 가능 시)

## 10. 파일 포맷 및 업로드 규칙 (A/B 파일)

* **A파일(요약, 마스터):** CSV 또는 XLSX
    * **필수 컬럼(예시):**
        * `프로젝트명`, `기획사명`, `기본계약개월`, `연장개월`, `계약시작일(YYYY-MM-DD)`, `최초투자금`, `추가투자금`, `총회수금`, `음반매입원가`, `BM구분(선급/일반/OST/음반)`
    * **제약:** 금액은 원 단위 정수, 음수 불가(환입 시 별도 규칙 정의 전까지 0 고정)
* **B파일(원본, 일/곡/앨범 단위):** CSV 또는 Parquet 권장
    * **필수 컬럼(예시):** `프로젝트명`, `일자`, `곡ID`, `앨범ID`, `매출`, `원가(지급금)`, `정산구분`
    * **월 집계 규칙:** `yyyymm = substr(일자, 1, 7)` 기준으로 집계하여 `cashflow_monthly` 적재
* **키 매핑 규칙:**
    * `project.project_name` = 파일 내 `프로젝트명` (사전 정합성 가정)
* **업로드 트랜잭션:**
    * 업로드 시작 시 `upload_job` 생성 → 임시 테이블 적재 → 검증 통과 시 기존 본테이블 `TRUNCATE` 후 일괄 마이그레이션(마스터 방식)
    * 실패 시 전체 롤백, 기존 데이터 유지
* **검증(Validation):**
    * **A2A 검증:** 스키마/타입/필수 컬럼/도메인 범위 체크(예: 금액 ≥ 0, 날짜 유효성), 파일 해시 저장
    * **관계 검증:** A/B 간 `프로젝트명` 교차 존재 비율 보고(경고만, 차단 아님)
* **OCR/문서 취득(선택):** PDF 원천자료가 있을 경우 **Adobe Acrobat**을 통해 열람/추출(Edge 대신). OCR 파이프라인은 2단계 고도화 대상.

## 11. 연동 규격 (API/서비스 계약)

* **프론트엔드-백엔드(tRPC, Next.js):**
    * **네임스페이스:** `dashboard`, `simulation`, `risk`, `analysis`, `upload`
    * **예시 프로시저:**
        * `dashboard.getSummary(): { totalInvestment, recoupRate, riskCount, yoy }`
        * `dashboard.getBusinessComparison(): Array<{ businessType, revenue, profit, margin }>`
        * `risk.listWarnings(pagination): { items, total }`
        * `upload.startJob({fileMeta}) → jobId`, `upload.finishJob({jobId})`
* **AI 백엔드(FastAPI, Python/uv):**
    * `POST /forecast`
        * Request: `{ "series": number[12], "months": number(<=36), "model": "auto"|"arima"|"prophet" }`
        * Response: `{ "moderate": number[], "meta": { "model": string, "rmse": number, "mape": number } }`
    * **성능/타임아웃:** 서버 타임아웃 5s, p95 < 2s 목표
    * **에러 규격:** `{ "error": { "code": string, "message": string } }`
* **계층 분리:** Next.js(tRPC) ↔ FastAPI는 내부 REST 호출, 재시도/서킷브레이커 적용(프론트 서버단)

## 12. 계산 로직 명세

* **이익 계산(사업별):**
    * `선급투자`: `총회수금 - (최초투자금 + 추가투자금)`
    * `일반투자`: `매출 - 원가`
    * `음반(MD)`: `총매출 - 총 매입원가`
    * `OST`: `매출 - 원가(투자금)` (감가상각 단순화 유지)
* **DCF 계산:**
    * 입력 CF: `Worst = 0.9 * Moderate`, `Best = 1.1 * Moderate`
    * 할인율: 고정 `WACC` (월 단위로 변환하여 적용)
    * 식: `DCF = Σ_{t=1..M} CF_t / (1 + r)^t` (r = 월 WACC)
* **BEP(손익분기) 계산:**
    * 누적 할인현금흐름이 0 이상이 되는 최초 t(개월)
    * 불가능 시 `null` 반환
* **예측 모델 선택:**
    * 후보: ARIMA, Prophet, Naive(백업)
    * 기준: AIC/BIC/검증 RMSE/시즌성 유무 자동판단, 실패 시 Naive로 폴백
    * 평가 지표: RMSE, MAPE(12개월 백테스트, 시계열 블록 CV)

## 13. 보안/컴플라이언스 및 운영

* **접근 제어:** MVP는 권한 분리 미적용이나, 사내망/BASIC 접근 제한 적용
* **데이터 보호:** 금액/계약 데이터 외 PII 최소화, 필요 시 마스킹
* **비밀정보 관리:** 환경변수/시크릿은 Vault 또는 OS Secret Store, 저장 시 암호화
* **백업/복구:** 업로드 직전 DB 스냅샷(로컬/온프렘), 일 1회 자동 백업
* **데이터 보존:** 업로드 파일/해시는 12개월 보존, 로그는 90일 보존

## 14. 로깅/모니터링/알림

* **로그:** 구조화 JSON 로그(요청ID, 사용자, jobId, 소요시간, 에러코드)
* **메트릭:** 업로드 처리시간, 예측 실패율, 대시보드 응답지연 p95
* **트레이싱:** OpenTelemetry(선택), 서비스간 요청 연계ID
* **알림:** 업로드 실패/모델 오류 Slack 또는 메일 알림(온프렘 SMTP)

## 15. 개발환경/CI/CD

* **언어/런타임:** Node 20.x, Python 3.11+
* **패키지 매니저:** `Bun`(웹/모노레포), 파이썬 `uv` 선호
* **품질 도구:** TypeScript strict, ESLint, Prettier, Python `ruff`/`black`
* **형상/브랜치:** trunk-based, `feat/`, `fix/`, `chore/` 규칙
* **CI:** PR 시 타입체크/린트/유닛테스트, 문서 링크 미리보기
* **배포:**
    * **C(Local):** Turborepo 기반 로컬 실행(웹 SQLite), FastAPI 별도 프로세스
    * **B(On-prem):** 단일 VM 또는 K8s Namespace, DB(PostgreSQL 전환 고려), 헬스체크/롤백 스크립트
    * **A(Cloud 2단계):** Vercel(웹)/ECS·GKE(백엔드) 이관 고려

## 16. 테스트 전략 및 수용 기준

* **유닛 테스트:**
    * 계산 모듈(DCF/BEP), 리스크 분류 규칙, 업로드 파서/검증기
* **통합 테스트:**
    * A/B 파일 업로드 → DB 적재 → 대시보드 지표 일치성 확인
    * 예측 API 호출 → 응답 구조/성능(p95 < 2s) 검증
* **E2E 테스트:**
    * 운영자 업로드 → 대시보드 반영 → 리스크 팝업 표시
* **수용 기준(샘플):**
    * A파일 1만 행, B파일 50만 행 업로드가 5분 내 완료(온프렘 기준)
    * 대시보드 첫 페이지 p95 < 500ms(캐시 후)
    * 예측 응답 실패율 < 0.5%

## 17. 리스크 & 가정

* **가정:** `프로젝트명`은 사전 표준화되어 있으며 중복 없음
* **리스크:**
    * `프로젝트명` 불일치 → 업로드 후 교차 존재율 리포트로 탐지(경고)
    * 예측 정확도 미달 → 모델 폴백/하이브리드 집계, 경영지표는 예측값 별도 표기
    * 온프렘 자원 제한 → 배치 시간 조정/단순화 캐시 도입

## 18. 용어집

* **WACC:** 가중평균자본비용, 월 할인율로 변환하여 적용
* **DCF:** 할인현금흐름, 미래 CF를 할인하여 현재가치화한 합
* **BEP:** 손익분기 시점(개월), 누적 할인현금흐름 0 이상 최초 시점
* **A2A 검증:** 파일 간/내 일관성 및 스키마/타입 검증 절차
* **차트인 수익:** 지정 순위권 내 체류 기간 동안 발생한 모든 수익

---

문서 버전: v0.2 → 다음 개정 시, 변경이력 표 추가 예정.

## 19. 분석 프레임워크 & KPI 정의

* **핵심 KPI(집계 레벨 = 월/사업/기획사/프로젝트 선택적):**
    * **누적 투자금:** `최초투자금 + 추가투자금`
    * **누적 회수율(ROI):** `총회수금 / (최초투자금 + 추가투자금)`
    * **이익(사업별 정의 적용):** 선급/일반/MD/OST 로직 참조
    * **이익률:** `이익 / 매출`
    * **회수 속도(개월):** 계약 시작 후 `총회수금`이 `총투자금`을 초과하기까지 경과 개월(미달 시 null)
    * **리스크 건수:** 경고 상태인 프로젝트 수
* **세그먼트/필터:** 기간(월/분기/연), 사업타입, 기획사, 프로젝트, 계약 상태(진행/만료), 투자 규모 버킷(quantile)
* **분석 축:**
    * **트렌드/YoY/DoD:** 월별 추세 + 전년동월 대비 증감률
    * **구성비/기여도:** 사업/기획사별 매출·이익 구성비, 전체 대비 기여도
    * **분포/상자그림:** ROI/이익률/회수기간 분포(왜도/이상치 탐지)
    * **상관/회귀:** 차트인 주 수 ↔ 수익, 투자 규모 ↔ 회수율 관계

## 20. 대시보드 설계(그래프 카탈로그)

### Module 1 (경영진) – 홈 대시보드
* **KPI 카드(3)**: 누적 투자금, 누적 회수율, 현재 리스크 건수
    * 형식: 카드 + 전년동월 대비 화살표/퍼센트, 미니 스파크라인(12개월)
* **사업별 성과 비교(막대 스택/그룹):**
    * 지표: 매출/이익/이익률
    * 상호작용: 범례 클릭 필터, 막대 클릭 시 해당 사업 Drill-down
* **월별 추세(면적선/이중축):** 매출(면적) + 이익률(선)
    * 보조선: 전년동월 값(회색 점선)
* **투자→회수 워터폴:** 총투자금 → 회수금 → 이익 단계별 가시화
* **리스크 현황(도넛 + 추세선):** 경고 비중, 월별 경고 건수 추세
* **Top 기획사 스캐터:** `투자금`(x) vs `회수율`(y), 버블=매출, 색=사업타입

### Module 3 (리스크) – 리스크 모니터링
* **경고 테이블:** 정렬/검색/태그(사업/기획사)
* **미니 꺾은선(최근 3개월 회수액):** 행 내 스파크라인
* **리스크 스코어 분해 막대:** 조건 기여(기간경과 vs 회수율 미달)
* **만기 캘린더 히트맵:** 월별 만료 예정 건수, 가까운 만기 강조
* **Aging 히스토그램:** 회수율 버킷별 프로젝트 분포(예: <30%, 30~50%, …)

### Module 4 (심층 분석)
* **차트인 가치 회귀 스캐터:** `차트인 주 수`(x) ↔ `차트인 수익`(y)
    * 추정선 + 95% CI 밴드, 모델 성능 표기(R^2, RMSE)
* **Top10 차트인 수익 바 + 누적선:** 파레토 곡선
* **기획사 ROI 분포 상자그림:** 사업타입별 비교 탭
* **코호트 히트맵:** 계약시작월 코호트별 t+개월 회수율(0~12개월)
* **시나리오 그래프:** 시뮬레이션 Best/Mod/Worst DCF 곡선 비교

## 21. 차트 스펙(데이터/쿼리/표현)

각 차트는 다음 스펙을 공통으로 가진다.
* **데이터 소스:** `fact_monthly_finance`(집계), `dim_project`, `dim_company`, `dim_business`, `dim_date`
* **필터:** 기간, 사업타입, 기획사, 프로젝트(선택), 계약상태
* **집계 단위:** 월 기본(YYYY-MM), 필요 시 분기/연 집계
* **포맷:** 금액=원(천단위 구분), 비율=%(소수 1~2), 날짜=YYYY-MM
* **상호작용:** 툴팁(세부 지표), 클릭 Drill-down, 범례 토글, 점선 전년동월 표시
* **성능:** p95 < 500ms(캐시), 쿼리 TTL 5분, 최초 로딩 skeleton 처리

예시 스펙 – “사업별 성과 비교”
* 메트릭: `sum(revenue)`, `sum(profit)`, `avg(profit_margin)`
* 차원: `business_type`
* 정렬: `sum(profit)` desc
* 표현: 그룹 막대(매출/이익) + 보조선(이익률), 색상은 사업타입 팔레트

예시 스펙 – “Top 기획사 스캐터”
* x: `sum(investment_total)`, y: `sum(recouped)/sum(investment_total)`
* 사이즈: `sum(revenue)`, 색: `business_type`
* 라벨: 상위 5개만 자동 라벨, 나머지 툴팁

## 22. 분석 모델링/탐지 로직

* **추세/계절성 분해:** STL로 월별 매출/회수금 분해(시각화 오버레이)
* **이상치 탐지:** 잔차 z-score > 3, 또는 이동평균 대비 30% 편차 마킹
* **기여도 분석:** 사업/기획사/프로젝트 레벨의 매출/이익 기여도 트리맵
* **단순 회귀:** 차트인 주 수 → 수익 회귀, 다중공선성 검토(VIF), 교차검증
* **세그먼트 비교:** 투자 규모 quantile(소/중/대)에 따른 ROI 분포 차이

## 23. 데이터 마트 설계(대시보드용)

* **팩트:** `fact_monthly_finance(project_id, yyyymm, revenue, cost, recoup, profit, profit_margin, investment_total)`
* **디멘전:** `dim_project(project_id, project_name, company_id, business_type, contract_start_date, base_months, extended_months)`, `dim_company(company_id, company_name)`
* **머티리얼라이즈 뷰:**
    * `mv_business_kpis(yyyymm, business_type, revenue, profit, margin)`
    * `mv_risk_counts(yyyymm, warning_count)`
    * `mv_company_agg(company_id, revenue, profit, roi)`
* **리프레시:** 업로드 완료 후 트랜잭션 내 재생성

## 24. 시각화 가이드

* **색상:** 사업타입 고정 팔레트, 경고=적색, 양호=청색/녹색
* **축/기준선:** 금액 축 0 기준선 표기, 비율 축 0~100% 범위
* **레이블:** 단위(억원/백만원) 자동 스케일, 툴팁에 원 단위 상세 표기
* **접근성:** 색맹 팔레트 대비 보장, 툴팁 키보드 접근

## 25. 대시보드 레이아웃(안)

* **1행:** KPI 카드 3개 + 리스크 도넛
* **2행:** 사업별 성과 막대(좌) / 월별 추세(우)
* **3행:** 워터폴(좌) / Top 기획사 스캐터(우)
* **글로벌 필터 바:** 기간, 사업타입, 기획사

## 26. 드릴다운/네비게이션 규칙

* **KPI/카드 클릭:** 리스크 건수 → Module 3 리스트로 이동(동일 필터 유지)
* **사업 막대 클릭:** Module 4 해당 사업 탭 + 필터 적용
* **스캐터 점 클릭:** 해당 기획사 상세 리포트(ROI 분포, 상자그림)로 이동

## 27. 쿼리/캐시 전략

* **사전 집계:** `mv_*` 뷰를 우선 조회 → 미존재 시 팩트 집계
* **캐시 계층:** 서버 메모리 캐시(5분 TTL) + 클라이언트 SWR(초 단위)
* **페이징/제한:** TopN(예: 50) 기본 제한, 추가 조회는 lazy-load

## 28. 추가 수용 기준(시각화)

* 모든 대시보드 위젯은 p95 < 500ms(캐시 후)로 렌더링
* 드릴다운 시 기존 필터 상태가 유지되어야 함
* 스파크라인/히트맵 등 소형 시각화는 모바일(협폭)에서도 가독성 확보

## 29. 전략 검토 – 사용자 니즈 반영 체크리스트

* **페르소나/JTBD 정합성:** 경영진/사업관리/심사역 각 Top 3 과업이 화면 흐름에 반영되어 있는가?
* **핵심 과업 완주성:** 모듈별 대표 시나리오(예: 경고 확인→상세→조치)가 3클릭 내 가능인가?
* **의사결정 산출물 일치:** 화면의 최종 숫자/리포트가 기존 결재 양식과 구조/지표 정의가 일치하는가?
* **데이터 신뢰성:** 업로드 후 주요 지표가 기존 수기 엑셀 대비 ±1% 이내로 일치하는가?
* **시간 절감 효과:** 기존 수작업 대비 처리시간 절감 목표(≥ 50%)가 측정 가능하게 정의되어 있는가?

## 30. 우선순위 스코어링 템플릿(RICE)

* **원칙:** RICE = (Reach × Impact × Confidence) ÷ Effort. 상위 점수 과제부터 착수.

```markdown
| 항목                         | Reach | Impact(1–3) | Confidence(0–1) | Effort(인일) | RICE  | 비고 |
|-----------------------------|-------|-------------|------------------|--------------|-------|------|
| 대시보드 총계+YoY           |       |             |                  |              |       |      |
| 리스크 목록+경고 로직       |       |             |                  |              |       |      |
| 구보 시뮬레이터(ARIMA)      |       |             |                  |              |       |      |
| 사업별 성과 비교            |       |             |                  |              |       |      |
| 차트인 가치 회귀 분석       |       |             |                  |              |       |      |
```

## 31. 모듈화 타당성 점검(경계/응집/격리)

* **데이터 소유권:** 공통 `project`/`cashflow_monthly`는 읽기 공유, 리스크 판단 산출물은 Module 3 소유
* **결합/응집도:** 예측(FastAPI) ↔ 웹(tRPC)은 REST 계약으로 분리, 실패 시 폴백/서킷브레이커 적용
* **변경 빈도 분리:** 업로드/ETL(월 1회) vs 조회(상시)를 마트/뷰로 분리, 캐시 TTL 관리
* **장애 격리/확장성:** 예측 서비스는 독립 스케일아웃, 장애 시 대시보드는 독립 가동

## 32. 단계별 로드맵 게이트(실현 가능성)

* **C(Local) 게이트:**
    * 업로드→집계→대시보드 지표 일치(±1%)
    * 시뮬레이터 Best/Mod/Worst + DCF/BEP 계산 정확성 단위 테스트 통과
    * 데이터 마트/뷰 자동 재생성 스크립트
* **B(On-prem) 게이트:**
    * 실제 데이터 규모 성능 목표 충족(대시보드 p95, 업로드 소요)
    * 알림/백업/복구 절차 운영 검증, 감사로그 적재 확인
* **A(Cloud) 게이트:**
    * 동시 사용자/트래픽 상향, 권한 분리 도입 준비, 모니터링 대시보드 완비

## 33. 모듈별 성공지표(KPI)

* **Module 1(대시보드):** 첫 화면 p95 < 500ms, 리스크 건수→상세 전환율 개선
* **Module 2(시뮬레이터):** 예측 응답 p95 < 2s, BEP 오차 중앙값 ≤ 2개월, 리포트 활용률 상승
* **Module 3(리스크):** 경고 탐지 재현율 ≥ 90%, 경고→조치 등록율 개선, 만기 사전 알림 도달율
* **Module 4(분석):** TopN 리포트 월간 조회수, 회귀 성능(RMSE) 기준 충족

## 34. 주요 리스크/의존성

* **데이터 표준화:** `프로젝트명` 충돌/누락 → 업로드 후 교차 매칭 리포트(경고)
* **예측 정확도:** 데이터 편향/계절성 약함 → 모델 폴백/집계 하이브리드, 성능 메트릭 노출
* **조직 채택:** 엑셀 관성 → 동일 양식 내보내기, 교육 및 챔피언 사용자 지정

## 35. 의사결정/커뮤니케이션 운영

* **ADR(Architecture Decision Record):** 주요 설계/정책 변경 로그 유지(간단 템플릿)
* **리뷰 주기:** 경영진/사업/심사역 합동 리뷰로 KPI 달성 점검 및 우선순위 재정렬
* **변경관리:** 대시보드 위젯/필터 변경은 버전 태깅 및 변경 노트 배포